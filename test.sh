# ZSH_PLUGINS='colorize cp safe-paste git ubuntu systemd laravel symfony aliases docker docker-compose rsync fzf-zsh-plugin zsh-autosuggestions zsh-syntax-highlighting'
# for plugin in $ZSH_PLUGINS; do
# PATTERN="\n\t$plugin$PATTERN"
# done 
# sed -i "s/plugins=(git)/plugins=($PATTERN\n)/g" $HOME/.zshrc

sed -o ""